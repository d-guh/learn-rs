# An I/O Project: Building a Command Line Program
We will build a command line program (a version of `grep`). Note `ripgrep` exists (very cool)

We will examine how to make command line tools use terminal features, environment variables, error messages, etc.
